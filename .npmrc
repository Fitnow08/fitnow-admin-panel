# Плоская раскладка node_modules (как у npm).
# Чинит "Cannot find native binding" у бандлеров (rolldown/rollup/esbuild):
# их нативные optional-бинды становятся доступны обычным резолвом Node.
node-linker=hoisted
